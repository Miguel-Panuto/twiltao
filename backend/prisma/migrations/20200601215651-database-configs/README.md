# Migration `20200601215651-database-configs`

This migration has been generated by Miguel Panuto at 6/1/2020, 9:56:51 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."posts" (
"content" text  NOT NULL ,"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"id" SERIAL,"updatedAt" timestamp(3)  NOT NULL ,"user_id" integer  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE TABLE "public"."users" (
"createdAt" timestamp(3)  NOT NULL DEFAULT CURRENT_TIMESTAMP,"email" text  NOT NULL ,"id" SERIAL,"name" text  NOT NULL ,"password" text  NOT NULL ,"updatedAt" timestamp(3)  NOT NULL ,
    PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "posts.user_id" ON "public"."posts"("user_id")

CREATE UNIQUE INDEX "users.email" ON "public"."users"("email")

ALTER TABLE "public"."posts" ADD FOREIGN KEY ("user_id")REFERENCES "public"."users"("id") ON DELETE CASCADE  ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200601215651-database-configs
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,27 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url      = env("DATABASE_URL")
+}
+
+model posts {
+  id        Int      @default(autoincrement()) @id
+  content   String
+  user_id   Int      @unique
+  users     users    @relation(fields: [user_id], references: [id])
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
+
+model users {
+  id        Int      @default(autoincrement()) @id
+  email     String   @unique
+  name      String
+  password  String
+  posts     posts[]
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+}
```


